services:
  - type: web
    name: personal-site-preview
    runtime: node
    plan: free
    buildCommand: pnpm install && pnpm build
    startCommand: node deploy/render-server.js
    envVars:
      - key: PREVIEW_USER
        sync: false
      - key: PREVIEW_PASS
        sync: false
